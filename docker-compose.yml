services:
  finally:
    build: .
    image: finally_agents
    container_name: finally_agents
    ports:
      - "${PORT:-8000}:8000"
    volumes:
      - finally-data-agents:/app/db
    env_file:
      - .env
    restart: unless-stopped

volumes:
  finally-data-agents:
